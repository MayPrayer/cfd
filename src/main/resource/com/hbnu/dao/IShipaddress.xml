<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hbnu.dao.IShipaddress">
    <select id="selectaddrById" resultType="com.hbnu.entity.Shipaddress">
        select * from shipaddress where id =#{id}
    </select>
</mapper>